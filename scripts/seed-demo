#!/usr/bin/env bash
set -euo pipefail
repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_dir"
docker compose up -d --wait postgres
docker compose exec -T postgres psql -U company_os -d company_os < infra/postgres/demo/seed.sql
